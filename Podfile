# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Pandlar' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

pod 'ImagePicker'
pod 'Cosmos', '~> 16.0'

  # Pods for Pandlar

  target 'PandlarTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PandlarUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
